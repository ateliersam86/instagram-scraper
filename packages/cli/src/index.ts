#!/usr/bin/env node
/**
 * @atelier/instagram-scraper-cli
 *
 * Phase 0 placeholder. Subsequent phases wire real commands:
 * - `auth login` / `auth status`
 * - `profile <username>`
 * - `post <shortcode>`
 * - `stories <username>` (24h archive)
 * - `sync <username> --days N`
 */

console.error("instagram-scraper CLI is not implemented yet — see docs/roadmap.md.");
process.exit(2);
